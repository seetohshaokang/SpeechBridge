/**
 * ~2× scale for `<UserButton />` (avatar trigger + account dropdown).
 * Clerk defaults (Theme variables): fontSize md ≈ 0.8125rem, spacing 1rem,
 * borderRadius 0.375rem — see Clerk appearance docs.
 */
export const clerkUserButtonAppearance = {
  variables: {
    fontSize: "1.625rem",
    spacing: "2rem",
    borderRadius: "0.75rem",
  },
};
