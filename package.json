{
  "name": "speechbridge",
  "private": true,
  "type": "module",
  "scripts": {
    "convex:dev": "cd frontend && convex dev",
    "convex:deploy": "cd frontend && convex deploy",
    "convex:dashboard": "cd frontend && convex dashboard"
  },
  "dependencies": {
    "convex": "^1.34.0"
  }
}
